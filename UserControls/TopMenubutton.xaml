<UserControl x:Class="Project_C14.UserControls.TopMenubutton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" d:DesignWidth="100" Name="TopMenuButton">
    <Button Style="{DynamicResource TopMenuButton}">
        <Grid ClipToBounds="True">
            <Icon:PackIconMaterial Kind="{Binding ElementName=menuButton,Path=Icon}" Style="{StaticResource TopMenuButtonIcon}"/>
        </Grid>
    </Button>
</UserControl>